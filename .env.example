# ============================================================
# FarmTech Solutions - Environment Configuration
# Copie este arquivo para .env e preencha com suas credenciais
# ============================================================

# ==================== Banco de Dados ====================

# Tipo de Banco (true = SQLite, false = Oracle)
SQL_LITE=true

# SQLite (usado quando SQL_LITE=true)
DATABASE_URL=sqlite:///data/agro.db

# Oracle FIAP (usado quando SQL_LITE=false)
ORACLE_DSN=oracle.fiap.com.br:1521/ORCL
ORACLE_USER=seu_usuario_rm
ORACLE_PASSWORD=sua_senha

# ==================== AWS Credentials ====================

# AWS Access Keys (obtenha no IAM Console)
AWS_ACCESS_KEY_ID=AKIA...
AWS_SECRET_ACCESS_KEY=abc123...
AWS_REGION=us-east-1

# AWS SNS (Sistema de Alertas)
SNS_TOPIC_ARN=arn:aws:sns:us-east-1:123456789:farm-alerts

# AWS SES (Email - opcional)
SES_SENDER_EMAIL=noreply@farmtech.com
SES_VERIFIED_EMAILS=contato@farmtech.com,admin@farmtech.com

# ==================== Aplicação ====================

# Logging
LOGGING_ENABLED=true
LOG_LEVEL=INFO
LOG_FILE=logs/farmtech.log

# API IoT (Fase 3)
ENABLE_API=true
API_HOST=0.0.0.0
API_PORT=8180
API_WORKERS=4

# Dashboard (Fase 4)
DASHBOARD_PORT=8501
DASHBOARD_THEME=light  # light ou dark

# ==================== APIs Externas ====================

# API Meteorológica (WeatherAPI, OpenWeatherMap, etc)
WEATHER_API_KEY=sua_chave_api
WEATHER_API_URL=https://api.openweathermap.org/data/2.5/weather
DEFAULT_CITY=Campinas,SP

# ==================== Machine Learning ====================

# Modelos (Fase 4)
ML_MODEL_PATH=src/fase4/ml/models/
ML_CACHE_ENABLED=true

# YOLO (Fase 6)
YOLO_MODEL_PATH=src/fase6/models/best.pt
YOLO_CONFIDENCE_THRESHOLD=0.5
YOLO_IOU_THRESHOLD=0.5

# ==================== IoT & Sensores ====================

# ESP32 Configuration
ESP32_WIFI_SSID=sua_rede_wifi
ESP32_WIFI_PASSWORD=senha_wifi
ESP32_API_ENDPOINT=http://SEU_IP_LOCAL:8180

# Thresholds de Alerta
UMIDADE_MIN=30  # % - abaixo dispara alerta
UMIDADE_MAX=80  # % - acima dispara alerta
PH_MIN=5.5      # pH ideal mínimo
PH_MAX=7.5      # pH ideal máximo
TEMP_MIN=10     # °C - temperatura mínima
TEMP_MAX=35     # °C - temperatura máxima

# ==================== Segurança ====================

# Secret Key (para sessões, tokens, etc)
SECRET_KEY=gere-uma-chave-secreta-aleatoria-aqui

# JWT (se implementar autenticação)
JWT_SECRET_KEY=outra-chave-secreta-para-jwt
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# ==================== Desenvolvimento ====================

# Debug Mode
DEBUG=false
TESTING=false

# Cache
CACHE_TYPE=simple  # simple, redis, memcached
CACHE_REDIS_URL=redis://localhost:6379/0

# ==================== Outros ====================

# Timezone
TZ=America/Sao_Paulo

# Número de workers (processos paralelos)
WORKERS=4

# Upload de imagens (Fase 6)
UPLOAD_FOLDER=data/uploads
MAX_UPLOAD_SIZE_MB=10

# Retenção de dados (dias)
DATA_RETENTION_DAYS=365

# ==================== Notas de Segurança ====================
# 
# IMPORTANTE:
# 1. NUNCA commite o arquivo .env no Git (já está no .gitignore)
# 2. Use senhas fortes e únicas
# 3. Rotacione as chaves AWS periodicamente
# 4. Em produção, use AWS Secrets Manager ou Parameter Store
# 5. Conceda apenas as permissões IAM mínimas necessárias
#
# ============================================================
